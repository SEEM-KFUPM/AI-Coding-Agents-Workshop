# Instructions for working with the codebase

- for testing and development, always use the prepared conda environment specified in `environment.yml`
- do not modify the `input/` folder, it contains the original inputs and task context
